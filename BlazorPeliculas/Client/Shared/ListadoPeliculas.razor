 @if (Peliculas == null)
    {
        @if (Cargando==null)
       {
        <text>Cargando....(componente)</text>
    }
    else
    {
        @Cargando
    }
}
    else if (Peliculas.Count == 0)
    {
        @if (NoHayRegistros==null)
       {
        <text>No hay registros para mostrar(Componente)</text>
    }
    else
    {
        @NoHayRegistros
    }
        
    }
    else
    {
        <input type="checkbox" @bind="MostrarBotones" />
        @foreach (var item in Peliculas)
        {
            <PeliculaIndividual Pelicula="item" EliminarPelicula="EliminarPelicula"
            MostrarBotones="MostrarBotones"/>
        }
    }
@code {

    [Parameter] public List<Pelicula> Peliculas{ get; set; }
    [Parameter] public RenderFragment Cargando{ get; set; }
    [Parameter] public RenderFragment NoHayRegistros{ get; set; }
    bool MostrarBotones = true;

    void EliminarPelicula(Pelicula pelicula)
    {
        Peliculas.Remove(pelicula);
        Console.WriteLine($"Eliminando la pelicula{pelicula.Titulo}");
    }

    

}
